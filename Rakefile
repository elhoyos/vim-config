desc "Cretes needed symlinks"
task :link do
    sh "ln -s ~/.vim.local/gvimrc.local ~/.gvimrc.local"
    sh "ln -s ~/.vim.local/janus.rake ~/.janus.rake"
    sh "ln -s ~/.vim.local/vimrc.local ~/.vimrc.local"    
end
